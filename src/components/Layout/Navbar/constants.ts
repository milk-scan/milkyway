export type NavButton = {
    label: string;
    link: string;
}

export type UserDropdownItem = {
    label: string;
    link: string;
    icon?: string;
}

export type Profile = {
    photo: string;
    name: string;
};